const fs = require('fs'); const path = require('path'); const dir = 'src/content/blog'; const polishMonths = { 'stycznia': '01', 'lutego': '02', 'marca': '03', 'kwietnia': '04', 'maja': '05', 'czerwca': '06', 'lipca': '07', 'sierpnia': '08', 'wrzeœnia': '09', 'paŸdziernika': '10', 'listopada': '11', 'grudnia': '12' }; const files = fs.readdirSync(dir).filter(f => f.endsWith('.md')); for (const f of files) { const p = path.join(dir, f); let content = fs.readFileSync(p, 'utf8'); if (!content.includes('date: ')) { const match = content.match(/updatedAt: "(.*?)"/); if (match) { const parts = match[1].split(' '); if (parts.length === 3) { let d = parseInt(parts[0], 10).toString().padStart(2, '0'); let m = polishMonths[parts[1].toLowerCase()]; if (!m) { m = '01'; console.log('Unknown month', parts[1]); } let y = parts[2]; const dateStr = 'date: "' + y + '-' + m + '-' + d + '"\n'; content = content.replace(/updatedAt: "(.*?)"\n/, dateStr + 'updatedAt: ""\n'); fs.writeFileSync(p, content); console.log('Updated', f); } } } }
